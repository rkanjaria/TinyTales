package com.rk.tinytales.book.data.mappers

/*
import com.ai.askera.chat.data.local.entity.ConversationEntity
import com.rk.tinytales.book.data.local.entity.MessageEntity
import com.rk.tinytales.book.domain.models.Conversation
import com.rk.tinytales.book.domain.models.Message

fun MessageEntity.toMessage(): Message {
    return Message(
        id = id,
        conversationId = conversationId,
        message = message,
        messageFrom = messageFrom,
        createdAt = createdAt,
        updatedAt = updatedAt
    )
}

fun ConversationEntity.toConversation(): Conversation {
    return Conversation(
        id = id,
        title = title,
        createdAt = createdAt,
        updatedAt = updatedAt
    )
}
*/


